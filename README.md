# VRFunScriptCast

本项目由我个人借助 AI 工具开发。个人精力的付出、AI 工具的使用都有成本，而项目**免费**提供给大家使用。

欢迎大家体验，并提出 **BUG 反馈、功能需求、优化建议**。也欢迎大家投喂白饭给大肥鱼，助力项目开发。

<p align="center">
  <img src="docs/sponsor/sponsor.jpg" alt="梗图与赞助码" width="880">
</p>

**沟通渠道**：QQ `2831691505`

---

**VR 版 FunScriptCast** —— Meta Quest / Pico 双平台 VR 媒体播放器与 funscript 同步应用。
由手机版 [FunScriptCast](https://github.com/wanfneg/FunScriptCast) 移植而来：戴上头显浏览本地与局域网媒体库、观看 2D/全景/3D 视频，并让实体玩具设备（ServeU、Vorze 等遵循官方/第三方协议的产品）按 funscript 与画面精确同步。

![platforms](https://img.shields.io/badge/platform-Quest%203%20%7C%20Pico-8A2BE2) ![unity](https://img.shields.io/badge/Unity-2022.3.62t13%20(Tuanjie)-333333)

**安装包下载**：[Releases](https://github.com/wanfneg/VRFunScriptCast-Releases/releases)

---

## 功能总览

### 视频播放
- 视频源：头显本地文件夹 + 局域网网络源（**SMB / WebDAV / DLNA**），统一硬解播放管线，支持缓冲重试与任意拖动
- 投影格式：平面 / 经纬图 180° / 经纬图 360° / 鱼眼 180° / 鱼眼 190° / 镜头轮廓 200° / 镜头轮廓 220°，播放中随时切换
- 立体格式：上下 / 左右半宽 / 左右全宽 / 单视场
- 播放窗口位置与朝向自由调整；多款内置虚拟场景背景，可一键切换头显真实环境透视背景
- 视频透传（MR）：色键抠像（绿 / 蓝 / 灰 + 自动识色）与 Packed-Alpha 蒙版直通，绿幕片源的人物可以叠加在真实房间中

### funscript / 设备同步
- 脚本自动匹配：视频同目录同名 → 本地脚本文件夹 → 网络源同目录 → 远程脚本源，四级链路自动查找；也支持手动点选
- 预设模式：9 种波形预设 + RANDOM 循环随机 + BOOST 狂暴加速；行程范围、速度滑条实时可调
- 快捷动作：一键急停 / 一键爆发 / 待机缓动 / 联动控制
- 脚本设置：延迟调整、跳过无动作段、手动位置
- 设备管理：蓝牙连接、行程范围 / 限速 / 反转 / 狂暴模式（OC）、设备信息、WiFi 固件 OTA 升级

### AI 字幕（需 PC 端字幕服务）
- 语音识别 + 中文翻译，实时逐句显示；识别中途先行出字，语句定稿后自动覆盖
- 源语言支持日语 / 英语；PC 端模型预热就绪后自动继续播放

### 媒体库与文件
- 媒体库快捷收藏：本地文件夹与网络源卡片，一键直达；浏览位置自动记忆，从哪来回哪去
- 文件浏览：目录 / 视频 / 脚本 / 字幕混排列表，按类型图标与颜色区分
- 外挂字幕：.srt / .ass / .ssa 同目录同名自动加载

### VR 交互
- 激光指针 + 白点高亮，扳机点击、B 键返回
- 摇杆左右轻拨 ±10 秒跳转、按住持续快进快退（带画面预览）；上下推动推拉画面变焦
- grip 键按住拖动，自由移动 / 转动播放窗口
- 进度条以 funscript 动作热力图呈现，一眼看到"哪里有动作"

---

## 使用教程

### 1. 安装

从 [Releases](https://github.com/wanfneg/VRFunScriptCast-Releases/releases) 下载对应平台的 APK：

| 设备 | 安装包 |
|---|---|
| Meta Quest（含 Quest 3） | `VRFunScriptCast-Meta.apk` |
| Pico | `VRFunScriptCast-Pico.apk` |

安装方式任选其一：

- 电脑上用 SideQuest 侧载安装（Quest 需先开启开发者模式）；
- 或用 adb：`adb install -r VRFunScriptCast-Meta.apk`。

首次启动会请求**存储权限**，用于浏览本地视频；拒绝后将看不到本地文件。

### 2. 主页导览

启动后进入主页，底栏四个入口：

| 入口 | 内容 |
|---|---|
| **媒体库** | 快捷收藏：本地文件夹与网络源卡片，点开即浏览 |
| **本地文件** | 浏览头显本地存储，选定视频文件夹 / 脚本文件夹 |
| **本地网络** | 配置 SMB / WebDAV / DLNA 的视频源与脚本文件源 |
| **设置** | 播放控制、脚本、设备、AI 字幕、固件更新等分页 |

主页左上角是**设备胶囊**（状态点 + 设备名），点按即可连接 / 断开蓝牙设备。

### 3. 播放视频

**本地视频**：从「本地文件」或媒体库收藏的文件夹进入，点视频即播。

**网络视频**：先在「本地网络」页配置视频源（每行协议一张卡片，点「配置」填写）：

- **WebDAV**：服务器地址（完整 URL，如 `https://dav.example.com/dav/...`）、账号、密码（可空）
- **SMB**：主机地址（IP，如 `192.168.1.5`）、共享名（**可空** —— 留空会自动列出该账号可访问的共享，含常见盘符与用户/媒体目录）、账号、密码
- **DLNA**：点「扫描设备」从发现的列表中选择；扫不到时手填设备描述地址（如 `http://192.168.1.5:8200/dd.xml`）

配置后可点「文件夹」选定进入该源的默认目录。SMB 大文件拖动进度条到中后段也很快（定位读取不做全量跳过）。

**播放页「切换视频」**：不回主页直接换片，可在当前目录继续挑选，网络源同样支持。

### 4. 播放控制与手柄操作

| 操作 | 效果 |
|---|---|
| 扳机 | 点击 / 确认 |
| B 键 | 返回 |
| Y 键 | 取消选择 / 关闭弹窗 |
| 摇杆左右轻拨 | 快退 / 快进 10 秒 |
| 摇杆左右按住 | 持续快进快退（进度预览，松手落定） |
| 摇杆上下 | 推拉画面（拉近 / 拉远；360° 全景不支持） |
| grip 按住拖动 | 移动 / 转动播放窗口 |

播放页界面分区：

- **底部按钮**：主页 / 切换视频 / 一键爆发 / 一键急停 / 待机缓动 / 联动控制 / 预设模式 / 视频透传
- **顶栏右侧**：透视（真实环境背景开关）、场景（循环切换内置虚拟场景）、CC（文件字幕开关）
- **顶栏左侧**：设备连接胶囊，播放中也能随时连接 / 断开设备
- **格式行**：投影 / 立体 / AI 字幕 三个下拉胶囊
- **进度条**：funscript 动作热力图 + 可抓取的三角把手

### 5. 视频画面格式

播放页顶部「投影」「立体」两个下拉，播放中随时切换。常见组合：

| 片源 | 投影 | 立体 |
|---|---|---|
| 普通 2D 视频 | 平面 | 单视场 |
| VR180（左右立体） | 经纬图 180° | 左右全宽 |
| VR180（上下立体） | 经纬图 180° | 上下 |
| 鱼眼 VR190 | 鱼眼 190° | 按片源选左右全宽 / 上下 |
| 镜头轮廓标定片源 | 镜头轮廓 200° / 220° | 按片源选择 |

带 Alpha 蒙版的特殊片源会自动切换到对应格式并开启透视。格式仅对当前视频生效，换片后按新片源重新选择。

### 6. 字幕与 AI 字幕

**文件字幕**：把 `.srt` / `.ass` / `.ssa` 与视频放在同一目录并同名，播放时自动加载；顶栏 CC 图标开关显示。

**AI 字幕**（实时语音识别 + 翻译，需 PC 端服务）：

1. PC 与头显接入**同一局域网**，在 PC 上启动字幕服务（默认端口 **8756**）
2. 主页 → 设置 → **AI 字幕**：打开「启用 AI 字幕」总开关，填服务地址（`http://PC的IP:8756`）
3. 播放页点「AI 字幕」按钮 → 选择源语言（日语 / 英语）；再点一次关闭
4. 首次开启会等 PC 端模型预热（识别 + 翻译），就绪后**自动继续播放**；识别过程中字幕先行显示临时文本，语句定稿后自动替换，译文为中文

### 7. funscript 设备同步

**连接设备**

1. 设备开机、放在头显附近；首次使用按提示授予蓝牙权限
2. 主页（或播放页）左上角设备胶囊点按连接，再点断开
3. 设置 → **设备信息**可查看硬件版本、固件版本、行程范围、最大速度

**脚本加载**（自动，按顺序尝试）：

① 视频同目录同名 `.funscript` → ② 本地脚本文件夹（设置 → 脚本设置中指定，自动建立索引）→ ③ 网络源视频同目录 → ④ 远程脚本文件源（「本地网络」页配置，需选定脚本文件夹）。

也可以在文件列表 / 切换视频中**手动点选**脚本，选择会保留到被播放使用或手动清除。

**脚本设置**（设置 → 脚本设置）：

- 延迟调整：脚本整体提前 / 推后，补偿设备响应
- 跳过无动作：脚本连续无动作达到设定时长时，自动快进该段
- 手动位置：手动指定脚本作用位置

**设备高级**（连接设备后可用）：行程范围（双滑条限定最小 / 最大行程，上下限自动归序防交叉）、限速、反转、狂暴模式（OC）。

### 8. 预设模式与快捷动作

播放页底部「**预设模式**」：

- 内置 24 种预设（与手机版同源同名单）：标准往复、峰谷连环、温柔正弦、深浅跌宕、轻挑细捻、极限冲刺、强拍律动，以及拉链爬升、长波缓涌、深抽连颤、拱形中浪、低吟渐起、连击长落、细密渐强、冲顶碎颤、中幅爆发、锯齿大落、密颤变奏、呼吸起伏、逐级推移、蓄势突进、全幅大浪、潮汐涨落、匀速全抽
- **RANDOM**：循环随机切换波形；**BOOST**：狂暴加速
- 行程范围双滑条 + 速度滑条，可一键恢复默认速度

底部**快捷动作**：

- **一键急停**：立即停止设备运动，再点恢复
- **一键爆发**：触发一次爆发动作，再点停止
- **待机缓动**：设备空闲一段时间后轻柔往返（时长可调），避免完全静止
- **联动控制**：行程 / 速度 / 关联主输出集中调节

### 9. 视频透传（MR 抠像）

适用于绿幕 / 蓝幕实拍片源：抠掉纯色背景后，画面人物与头显透视出的真实房间叠加。

播放页底部「**视频透传**」打开弹窗：

- 模式：关闭 / 纯绿键 / 纯蓝键 / 中性灰键 / 浅灰键 / Alpha 直通 / 蒙版预览
- 滑杆实时调节键色阈值；「**自动识别**」从当前帧取主色作键色，免手动调色
- 「蒙版预览」：白色 = 保留前景，黑色 = 抠出透出
- 开启任一抠像模式会自动打开头显透视背景，真实环境从抠掉的区域透出

### 10. 常见问题

- **看不到本地视频**：存储权限未授予——到系统设置里给本应用存储权限后重进。
- **网络源连不上**：确认头显与 NAS / PC 在同一局域网；核对 IP、账号密码；SMB 可把共享名留空先列出可访问的共享。
- **设备连不上**：确认设备已开机、有电、未被其他手机 App 占用蓝牙；点设备胶囊重试。
- **AI 字幕不出字**：确认 PC 端服务已启动、服务地址端口正确（默认 8756）、模型预热完成（首次加载较慢）。
- **画面全黑或变形**：播放页切换投影 / 立体格式；播放失败会显示红色提示条。
